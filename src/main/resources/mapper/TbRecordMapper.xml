<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tuk.mina.dao.record.TbRecordDao">
    <insert id="insertRecord" parameterType="TbRecordVo">
        INSERT INTO TB_MINA_RECORD_L (recordFileId, projectId, textFileId,recordlanguage,recordMemo,createUserId, createDttm)
        VALUES (#{recordFileId}, #{projectId}, #{textFileId}, #{recordlanguage}, #{recordMemo}, #{createUserId}, GETDATE())
    </insert>
</mapper>